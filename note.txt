v2.0.0.er4 (early release)

** THIS IS AN EARLY RELEASE OF v2.0.0 **

added:
- new C apis for lime::json
- bash script to auto generate version header files and set version in cmake and meson
- added new test to test C apis
- new lime::json::Encode interface to allow custom struct to get parsed by json::encode

update:
- updated tests/test.py to handle C source file
- updated C apis
- updated build scripts to get version from 'version' script
- updated build scripts to install the generated version.h header files
- updated LimeHttpRequest functions to accept const LimeHttpRequest*

fixed:
- fixed a bug in lime::json::Array encoding
